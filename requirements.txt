# Flat pip-installable dependency list for the whole NVH stack.
# Each pyproject.toml declares its own deps for a real package build;
# this file exists so a developer can bootstrap the full workspace
# with just `pip install -r requirements.txt` without having to know
# the install order for the seven local packages.
#
# Usage from the repo root:
#   python -m venv .venv
#   . .venv/bin/activate
#   pip install -r requirements.txt
#   pip install -r requirements-dev.txt   # optional -- for pytest
#
# --------------------------------------------------------------------
# Third-party runtime deps (pinned to lower bounds only; pyproject
# files carry the same versions -- keep the two in sync when bumping).
# --------------------------------------------------------------------

# Contracts + validation
pydantic>=2.6
sqlalchemy>=2.0

# Numerics + signal processing
numpy>=1.26
scipy>=1.11
pyarrow>=15.0

# Backend
fastapi>=0.110
uvicorn[standard]>=0.29
httpx>=0.27
pyzmq>=25

# Desktop GUI
PySide6>=6.8

# --------------------------------------------------------------------
# Local packages (editable installs so tests can import + edits are
# live). Order matters: leaf packages first, then their consumers.
# --------------------------------------------------------------------

-e ./design-tokens
-e ./libs/nvh_contract
-e ./libs/nvh_api_schemas
-e ./analysis-engine
-e ./simulator
-e ./web-backend
-e ./qt-app
