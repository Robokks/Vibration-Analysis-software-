# Third-party dependencies for the whole NVH stack.
# Local packages install as a separate step -- see the follow-up
# `pip install -e ...` in the README (or run scripts/install_local.sh
# / .bat which does it for you). Splitting them keeps this file
# portable: some pip versions on Windows reject `-e ./path` inside a
# requirements file, so we don't put editable installs here.
#
# Usage from the repo root:
#   python -m venv .venv
#   . .venv/bin/activate       # Windows: .venv\Scripts\activate
#   pip install -r requirements.txt
#   pip install -e design-tokens -e libs/nvh_contract -e libs/nvh_api_schemas \
#               -e analysis-engine -e simulator -e web-backend -e qt-app

# Contracts + validation
pydantic>=2.6
sqlalchemy>=2.0

# Numerics + signal processing
numpy>=1.26
scipy>=1.11
pyarrow>=15.0

# Backend
fastapi>=0.110
uvicorn[standard]>=0.29
httpx>=0.27
pyzmq>=25

# Desktop GUI
PySide6>=6.8
